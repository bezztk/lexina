# Repository instructions

After every repository change, include the following in the final response:

- A concise suggested Git commit message describing the completed change.
- The exact number of added and removed lines for the change.

Calculate the line counts from the relevant Git diff. Keep unrelated pre-existing
working-tree changes out of the reported totals whenever the changes can be
distinguished reliably.
